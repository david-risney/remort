<Page x:Class="Remort.Devices.FavoritesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:devices="clr-namespace:Remort.Devices"
      Title="Favorites">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Favorites"
                   FontSize="28"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16" />

        <!-- Empty state -->
        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Visibility="{Binding HasNoFavorites, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ui:SymbolIcon Symbol="Star24" FontSize="48" Opacity="0.3" HorizontalAlignment="Center" Margin="0,0,0,12" />
            <TextBlock Text="No favorites yet" FontSize="18" Opacity="0.5" HorizontalAlignment="Center" Margin="0,0,0,4" />
            <TextBlock Text="Mark devices as favorites in the Devices view" Opacity="0.4" HorizontalAlignment="Center" />
        </StackPanel>

        <!-- Favorite cards -->
        <ItemsControl Grid.Row="1"
                       ItemsSource="{Binding Devices}"
                       Visibility="{Binding HasFavorites, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type devices:DeviceCardViewModel}">
                    <Button Margin="4"
                            Width="220"
                            Height="140"
                            Command="{Binding OpenCommand}"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="CardBorder"
                                        CornerRadius="8"
                                        Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                                        BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1"
                                        ClipToBounds="True">
                                    <ContentPresenter HorizontalAlignment="Stretch"
                                                      VerticalAlignment="Stretch" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="CardBorder" Property="Background"
                                                Value="{DynamicResource CardBackgroundFillColorSecondaryBrush}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <Grid>
                            <Border CornerRadius="8"
                                    Background="{Binding BackgroundBrush}"
                                    Opacity="0.3" />
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Top"
                                        Margin="0,4,4,0">
                                <Button Command="{Binding ToggleFavoriteCommand}"
                                        Click="CardButton_Click"
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        Padding="4"
                                        Cursor="Hand">
                                    <ui:SymbolIcon Symbol="Star24" FontSize="16">
                                        <ui:SymbolIcon.Style>
                                            <Style TargetType="ui:SymbolIcon">
                                                <Setter Property="Filled" Value="False" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                                                        <Setter Property="Filled" Value="True" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ui:SymbolIcon.Style>
                                    </ui:SymbolIcon>
                                </Button>
                                <Button Click="MoreButton_Click"
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        Padding="4"
                                        Cursor="Hand">
                                    <Button.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Toggle Favorite" Command="{Binding ToggleFavoriteCommand}" />
                                            <Separator />
                                            <MenuItem Header="Remove Device" Command="{Binding RemoveCommand}" />
                                        </ContextMenu>
                                    </Button.ContextMenu>
                                    <ui:SymbolIcon Symbol="MoreHorizontal24" FontSize="16" />
                                </Button>
                            </StackPanel>
                            <StackPanel VerticalAlignment="Bottom" Margin="12">
                                <TextBlock Text="{Binding Name}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis"
                                           MaxWidth="180" />
                            </StackPanel>
                        </Grid>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
